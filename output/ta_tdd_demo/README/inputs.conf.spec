[city_weather_input://<name>]
account = Select the account that provides the base_url and API key.
city = City to monitor (use lowercase when using Mockoon, e.g., guanacaste).
country_code = ISO 3166-1 alpha-2 country code (e.g., CR).  Default: CR
index = Target index for ingested events.  Default: default
interval = Time interval (in seconds) between input runs.  Default: 86400
sourcetype = Sourcetype used for indexed weather events.  Default: weather:current
